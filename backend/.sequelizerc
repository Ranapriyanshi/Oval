const path = require('path');

// Resolve paths relative to this config file so migrations run correctly
// whether you run `npm run migrate` from backend/ or from project root
const backendRoot = path.resolve(__dirname);

module.exports = {
  'config': path.join(backendRoot, 'src', 'config', 'database.js'),
  'models-path': path.join(backendRoot, 'src', 'models'),
  'seeders-path': path.join(backendRoot, 'src', 'seeders'),
  'migrations-path': path.join(backendRoot, 'src', 'migrations')
};
